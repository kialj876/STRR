export const useStrrAccountStore = defineStore('strr/account', () => {
  // TODO: was thinking we'd have something like this for the strr account dashboard
  // const { $strrApi } = useNuxtApp()

  // const role = ref<'host' | 'pm' | 'strata' | 'platform' | undefined>(undefined)
  // const applications = ref([])
  // const registrations = ref([])

  // function loadAccountApplications () {}

  // function loadAccountRegistrations () {}

  return {}
})
